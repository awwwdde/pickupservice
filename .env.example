# Локальная разработка
VITE_SITE_URL=http://localhost:5173
VITE_BACKEND_ORIGIN=http://localhost:8000

# Продакшен (перед npm run build задать в .env или CI):
# VITE_SITE_URL=https://pickupservice.moscow
# VITE_BACKEND_ORIGIN=https://pickupservice.moscow

# Фронт: укажите URL Django API (на проде — тот же домен, если nginx отдаёт SPA и проксирует /api/).

# Бэкенд (Django) — задаются в окружении сервера, не в Vite:
# BOOKING_TO_EMAIL=office@example.com
# CORS_ALLOWED_ORIGINS=https://pickupservicetest.netlify.app
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
# DEFAULT_FROM_EMAIL=noreply@example.com
#
# Telegram (бот для уведомлений о заявках):
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_WHITELIST_USER_IDS=760723570
# TELEGRAM_WHITELIST_CHAT_IDS=-1001234567890
# TELEGRAM_NOTIFY_CHAT_IDS=760723570,-1001234567890
#
# Если сервер в РФ и Telegram недоступен напрямую — включите прокси:
# TELEGRAM_PROXY_URL=http://user:pass@proxyhost:3128
# или (нужен пакет python-telegram-bot[socks]):
# TELEGRAM_PROXY_URL=socks5://user:pass@proxyhost:1080
# (опционально) отдельный прокси только для polling/getUpdates:
# TELEGRAM_GET_UPDATES_PROXY_URL=http://user:pass@proxyhost:3128
#
# Яндекс.Карты (синхронизация отзывов):
# YANDEX_MAPS_ORG_ID=123456789
# Запуск: python manage.py sync_yandex_reviews
